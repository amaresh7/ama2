node('MAVEN') {
    stage('SCM') {
      // git clone
	  git 'https://github.com/amaresh7/ama2.git'
	  sh label: '', script: 'echo "git clone is susscess"'
   }
   stage('CLEAN'){
       sh label: '', script: 'mvn clean'
   }
}
